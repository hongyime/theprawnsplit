<!-- Vitest-only stub for src/lib/NeoCard.svelte (CR-prawn-ui):
     The runes component's {@render children()} fails in jsdom because legacy
     components pass slots, not snippets. This stub uses a <slot /> so child
     text is present for structural assertions. -->
<div class={$$props.class ?? ''}><slot /></div>
