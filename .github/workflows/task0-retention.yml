name: Task 0 — relay retention probe

on:
  schedule:
    - cron: "0 6 * * *"   # daily 06:00 UTC
  workflow_dispatch:       # manual runs for the 1h datapoint

permissions:
  contents: write

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: node scripts/task0-retention.mjs check
      - run: node scripts/task0-retention.mjs check-slow
      - run: node scripts/task0-retention.mjs check-current
      - name: Commit results
        run: |
          git config user.name "task0-probe"
          git config user.email "actions@github.com"
          git add .agents/task0-retention.md .agents/task0-retention-slow.md .agents/task0-retention-current.md
          git diff --staged --quiet || git commit -m "chore(task0): retention probe $(date -u +%F)"
          git push
